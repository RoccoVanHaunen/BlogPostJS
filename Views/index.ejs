<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Blog</title>
    <link rel="stylesheet" href="/style.css">
</head>
<body>
    <h1>Blog Posts</h1>
    <% posts.forEach(post => { %>
        <article>
            <h2><%= post.title %></h2>
            <p><%= post.content %></p>
            <small><%= new Date(post.created_at).toLocaleString() %></small>
        </article>
    <% }); %>
    <a href="/admin">Admin Panel</a>
</body>
<h5>Created to support local comunity</h5>
</html>